import { Sidebar } from '@/components/sidebar';import { tickets } from '@/lib/sample-data';export default function Tickets(){return <main><Sidebar/><section className="ml-80 p-8"><h1 className="text-4xl font-semibold">Tickets</h1><div className="glass mt-8 rounded-3xl p-6">{tickets.map(t=><a href={`/tickets/${t.id}`} className="grid grid-cols-6 rounded-2xl p-4 hover:bg-white/5" key={t.id}><span>{t.id}</span><span>{t.customer}</span><span>{t.language}</span><span>{t.intent}</span><span>{t.priority}</span><span>{t.team}</span></a>)}</div></section></main>}
