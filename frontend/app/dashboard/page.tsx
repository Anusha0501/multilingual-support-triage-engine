'use client';
import { motion } from 'framer-motion';
import { Line, LineChart, ResponsiveContainer, Tooltip, XAxis } from 'recharts';
import { Sidebar } from '@/components/sidebar';
import { MetricCard } from '@/components/metric-card';
import { tickets, trend } from '@/lib/sample-data';
export default function Dashboard(){return <main><Sidebar/><section className="ml-80 p-8"><motion.div initial={{opacity:0,y:16}} animate={{opacity:1,y:0}}><p className="text-sm uppercase tracking-[0.3em] text-violet-300">Enterprise AI Support</p><h1 className="mt-3 text-5xl font-semibold">Multilingual triage cockpit</h1><p className="mt-4 max-w-2xl text-slate-400">Detect language, classify intent, predict SLA risk, and draft localized replies with full observability.</p></motion.div><div className="mt-8 grid grid-cols-4 gap-4"><MetricCard label="Open tickets" value="128" delta="+12%"/><MetricCard label="Automation" value="64%" delta="+8%"/><MetricCard label="SLA risk" value="17" delta="-4%"/><MetricCard label="Languages" value="12" delta="+3"/></div><div className="mt-6 grid grid-cols-3 gap-4"><div className="glass col-span-2 rounded-3xl p-6"><h2 className="text-lg font-medium">Ticket automation trend</h2><div className="mt-6 h-72"><ResponsiveContainer><LineChart data={trend}><XAxis dataKey="name" stroke="#94a3b8"/><Tooltip contentStyle={{background:'#0f172a',border:'1px solid #334155'}}/><Line type="monotone" dataKey="tickets" stroke="#a78bfa" strokeWidth={3}/><Line type="monotone" dataKey="automated" stroke="#34d399" strokeWidth={3}/></LineChart></ResponsiveContainer></div></div><div className="glass rounded-3xl p-6"><h2 className="text-lg font-medium">SLA heatmap</h2><div className="mt-5 grid grid-cols-7 gap-2">{Array.from({length:35}).map((_,i)=><div key={i} className="h-8 rounded-lg" style={{background:`rgba(124,58,237,${0.18+(i%7)*0.1})`}} />)}</div></div></div><div className="glass mt-6 rounded-3xl p-6"><h2 className="text-lg font-medium">Live triage queue</h2><div className="mt-4 divide-y divide-slate-800">{tickets.map(t=><div key={t.id} className="grid grid-cols-7 py-4 text-sm"><span>{t.id}</span><span>{t.customer}</span><span>{t.language}</span><span>{t.intent}</span><span>{t.priority}</span><span>{t.team}</span><span className="text-amber-300">{t.sla}% SLA risk</span></div>)}</div></div></section></main>}
